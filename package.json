{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "concurrently \"cd backend && npm run start:dev\" \"cd frontend && npm run dev\"",
    "build": "concurrently \"cd backend && npm run build && npx prisma db push\" \"cd frontend && npm run build\"",
    "start": "concurrently \"cd backend && npm run start:prod\" \"cd frontend && npm run preview\""
  }
}
