{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "concurrently -k \"cd frontend && npm run dev\" \"cd backend && npm run start:dev\"",
    "build": "concurrently -k \"cd frontend && npm run build\" \"cd backend && npm run build && npx prisma db push\"",
    "start": "concurrently -k \"cd frontend && npm run preview\" \"cd backend && npm run start:prod\""
  }
}